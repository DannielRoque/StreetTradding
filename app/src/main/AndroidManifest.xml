<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="br.edu.fatecgarca.freetrading">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.AdicionarClienteActivity"/>
        <activity
                android:name=".ui.LoginActivity"
                android:screenOrientation="portrait"/>
        <activity android:name=".ui.AdicionarProdutoActivity"/>
        <activity
                android:name=".ui.MainActivity"
                android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.ProdutosActivity"/>
        <activity android:name=".ui.ClientesActivity"/>
        <activity android:name=".ui.PendenciasActivity"/>
        <activity
                android:name=".popOut.Pop"
                android:theme="@style/AppTheme.CustomThere"/>
        <activity android:name=".ui.VendaActivity">
        </activity>
    </application>

</manifest>